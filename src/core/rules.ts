import type { RulesConfig } from './types'

export const defaultRules: RulesConfig = {
  gravityPerSecond: 1.2,
  lockDelayMs: 500,
  dasMs: 150,
  arrMs: 40,
  softDropMultiplier: 16,
  specialPieceChance: 0.05,
}
